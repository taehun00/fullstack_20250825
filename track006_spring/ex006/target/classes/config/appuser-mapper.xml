<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thejoa703.dao.AppUserDao">

	<resultMap id="AppUserMap" type="AppUserDto">
		<result property="appUserId" column="app_user_id"/>
		<result property="email" column="email"/>
		<result property="password" column="password"/>
		<result property="mbtiTypeId" column="mbti_type_id"/>
		<result property="createdAt" column="created_at"/>
		<result property="ufile" column="ufile"/>
	</resultMap>

	<insert	id="insert" parameterType="com.thejoa703.dto.AppUserDto">
		insert into appuser (app_user_id, email, password, mbti_type_id)
		
		values (appuser_seq.NEXTVAL, #{email}, #{password}, #{mbtiTypeId})
	</insert>
	<insert id="insert2" parameterType="com.thejoa703.dto.AppUserDto">
	  	insert into appuser (app_user_id, email, password, mbti_type_id, ufile)
		
		values (appuser_seq.NEXTVAL, #{email}, #{password}, #{mbtiTypeId}, #{ufile})
    </insert>
	<update id="update" parameterType="com.thejoa703.dto.AppUserDto">
		update appuser
		set email=#{email}, password=#{password}, mbti_type_id=#{mbtiTypeId}
		where app_user_id = #{appUserId}
	</update>
	<update id="update2" parameterType="com.thejoa703.dto.AppUserDto">
	  	update appuser
		set email=#{email}, password=#{password}, mbti_type_id=#{mbtiTypeId}, ufile=#{ufile}
		where app_user_id = #{appUserId}
    </update>
	<delete id="delete" parameterType="com.thejoa703.dto.AppUserDto">
		delete from appuser where email=#{email} and password=#{password}
	</delete>
	
	<select resultType="int" id="join" parameterType="string">
    	select count(*) from appuser where email = #{email}
	</select>
	
	<select resultMap="AppUserMap" id="login" parameterType="com.thejoa703.dto.AppUserDto">
    select *
    from appuser 
    where email = #{email} 
      and password = #{password}
	</select>
	
	<select resultMap="AppUserMap" id="select" parameterType="int">
		select * from appuser where app_user_id=#{appUserId}
	</select>
	<select resultMap="AppUserMap" id="selectAll">
		select * from appuser order by app_user_id desc
	</select>
	<!-- 관리자용 -->
	<delete id="deleteAdmin" parameterType="com.thejoa703.dto.AppUserDto">
		delete from appuser where app_user_id=#{appUserId}
	</delete>
	<update id="updateAdmin" parameterType="com.thejoa703.dto.AppUserDto">
	  	update appuser
		set mbti_type_id=#{mbtiTypeId}
		where app_user_id =#{appUserId}
    </update>
</mapper>